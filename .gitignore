node_modules
dist
.DS_Store
*.log
.vercel
# Deps are resolved via .npmrc (legacy-peer-deps); no lockfile is committed so
# Vercel's install resolution is not pinned by a locally-generated lock.
package-lock.json
